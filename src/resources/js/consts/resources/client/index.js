import * as MUTATIONS from './mutations';
import { APP } from '../default/urls';

const RESOURCE = 'client';
const URL = APP;
const LOADINGS = {};
const ACTIONS = {};

export {
  RESOURCE,
  MUTATIONS,
  ACTIONS,
  LOADINGS,
  URL,
};
